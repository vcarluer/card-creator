card-creator creates a printable A4 image composed of games card with a word inside and a background.
You can specify the path to text file containing the word list and the path to the background image.
Every image will have the same size.
